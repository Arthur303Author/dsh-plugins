# dsh bundle layer: mount the dsh-memory plugin (index.js via the package
# root export). Inserted as its own entry so the loader activates it after
# the tools registry; the plugin registers remember / recall / forget tools
# that persist plain-markdown notes across sessions.
- insert:
    - id: memory
      name: 'dsh-memory'
